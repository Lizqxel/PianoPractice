# Chord Sprint

コードネームを見て素早く鍵盤を押さえるための、MIDIキーボード対応ピアノコード練習Webアプリです。通常の練習履歴と設定はブラウザの `localStorage` に保存します。U-FRET任意曲の取得とMP3自動変換には、このPC上のローカルサービスを使用します。

**[公開版を開く](https://lizqxel.github.io/PianoPractice/)** · [GitHubリポジトリ](https://github.com/Lizqxel/PianoPractice)

- MIDI入力と画面鍵盤でリアルタイムにコードを判定
- U-FRETのコード列を転記し、動画プラスまたはSongleの実時刻へ自動同期
- 再生中のコードを追従表示し、弾いた音が合っているかMIDIでその場で確認
- 14日間プラン、コード別成績、外部DAWへのMIDI出力に対応

## 起動方法

Node.js 20以降を推奨します。

```bash
npm install
npm run dev
```

ターミナルに表示されたURL（通常は `http://localhost:5173`）をChromeまたはEdgeで開いてください。

品質チェック:

```bash
npm test
npm run build
```

## 「曲で弾く」

原曲を聴きながら現在・次・その次のコード、曲全体のコード譜、鍵盤上の押さえ方を表示します。曲名・アーティスト名で検索してU-FRETの候補を選ぶと、コード記号と並びを自動転記し、時刻付きコード譜を組み立てて練習画面を開きます。コードや時刻を手で書き写す必要はありません。

自動同期には次の2経路があります。

1. 選択したYouTubeがU-FRETの動画プラス対象と一致する場合は、動画プラスの開始時刻・コード切替・テンポ変更を取り込み、各コードの実時刻へ変換します。
2. 動画プラスがない場合や別のYouTubeを選んだ場合は、U-FRETのコード列と、その動画に対するSongleの解析結果を照合して自動整列します。

動画の再生・停止・シークにコード表示と自動スクロールが追従し、コード譜を押すと動画をその位置へ移動できます。スクロール速度をBPMだけから推測したり、歌詞行を一律4拍として扱ったりはしません。コード数と動画プラスの切替数が合わない場合や、Songleとの一致点が不足する場合は、ずれた譜面を作らず明示的に停止します。録音固有のわずかな固定ずれは、練習画面の同期補正で調整できます。

Songleの候補を直接選んだ場合は、Songleが解析したコードと時刻をそのまま利用します。どの経路でも参照元を練習画面に表示します。Songleの解析結果は非商用利用に限定され、研究成果に依拠する旨の表示が必要です。詳細は[Songle API利用規約](https://api.songle.jp/terms_of_use)を確認してください。

### 公開版とローカル版

公開版には「常夜燈 / PEOPLE1」のU-FRET通常版コードと動画プラスの拍マップを収録しています。この曲は公開版だけで、対象YouTubeと自動同期して試せます。Songleのタイトル検索・YouTube URL指定・MIDI読込もブラウザから利用できます。

任意のU-FRET検索結果または曲ページURLを直接取り込む場合はローカル版を起動してください。ローカルAPIは取得ごとにU-FRETの `robots.txt` を読み、対象URLが許可されている場合だけページを取得します。取得したページからはコード記号、並び、BPM、動画プラスの同期情報、参照URLだけを抽出し、歌詞は取り込みません。

手元のMP3/WAV/FLAC/OGG/M4AをMuScriptorでMIDI化する「AI解析」と、MIDIを直接読み込む方式も補助的に使用できます。音声とMIDIは外部へアップロードせず、このPC内だけで処理します。

### 自作コード譜を使う場合

コード進行は `G#m | C#m | F# | B` のように小節を `|` で区切ります。1小節に複数ある場合は `G Am | F G` のように空白で区切ると等分されます。より正確に合わせる場合は次のように開始時刻を指定できます。

```text
[0:12.5] G#m
[0:16.8] C#m
```

### AI解析を使うローカル版

初回だけPowerShellで次を実行してください。

```powershell
npm run local:setup
```

セットアップ中にブラウザが開くので、無料のHugging FaceアカウントでMuScriptorモデルの非商用ライセンスへ同意し、ターミナルでログインします。2回目以降は次だけで起動できます。

```powershell
npm run local:start
```

ローカルAPIのモックテストは、セットアップ後に `npm run local:test` で実行できます（モデルのダウンロードや推論は行いません）。

起動後、ChromeまたはEdgeで `http://127.0.0.1:8222` が自動的に開きます。既定はRTX GPU向けの `medium` / `cuda` です。モデルは起動中には切り替わりません。変更する場合は `.env` の `MUSCRIPTOR_MODEL=large` などを書き換えて再起動してください。

YouTubeは公式プレイヤーでの原曲再生専用です。音声のダウンロードや抽出は行いません。URL貼付とSongleタイトル検索にはAPIキーもローカル版も不要です。

YouTubeと解析用のMP3/MIDIには、同じ録音・同じ編集版を選んでください。イントロなどの固定ずれは練習画面の同期補正で直せます。

MuScriptorのコードはMIT、モデル重みはCC BY-NC 4.0です。このローカル変換機能は個人・非商用利用を前提としています。

## MIDIキーボードの接続

1. MIDIキーボードをUSBでPCへ接続し、電源を入れます。
2. ChromeまたはEdgeでChord Sprintを開きます。
3. 画面右上の「MIDI機器を接続」を押します。
4. ブラウザのMIDIアクセス確認を許可します。
5. 複数の入力機器がある場合は、右上の一覧から使用する機器を選びます。

MIDI機器がなくても、画面下のC2〜C6仮想鍵盤をマウスまたはタッチで操作できます。MIDI入力と仮想鍵盤のどちらでも内蔵音源が鳴り、同じコード判定が動作します。

## Studio One／外部DAW音源

音源設定を「Studio One／外部DAW」にすると、受信したMIDIを選択したMIDI出力へ転送します。内蔵鍵盤音は停止しますが、コード判定、画面上の鍵盤、練習記録、内蔵メトロノームはそのまま利用できます。「内蔵＋外部の両方」も選択できます。

Windowsでは仮想MIDIポートを作成し、次のように接続します。

1. 「Chord Sprint Out」という仮想MIDIポートを作成します。
2. Chord Sprintの入力に実物のMIDIキーボード、出力にChord Sprint Outを選びます。
3. Studio One 6の外部デバイスでNew Keyboardを追加し、Receive FromをChord Sprint Out、Send ToをNoneにします。
4. Instrument Trackを作成し、好きな音源とプリセットを選んでモニターをONにします。

ブラウザからStudio One内のVSTやプリセットを取得することはできません。音源とプリセットの選択はStudio One側で行います。MIDI入出力に同じポートを選ぶとループする可能性があるため、画面の警告が出た場合は設定を見直してください。

## 収録機能

- コード瞬発: 今日・白鍵・メジャー・マイナー・苦手・カスタム・全コードから出題範囲を選択
- コード別成績: 出題数・正解率・平均反応を保存し、苦手コードを自動抽出
- コード進行: 4種類の進行、全12キー、40〜160 BPM、Web Audio基準の同期メトロノームと4カウント
- 60秒コードチェンジ: 共通音を残した滑らかなコード移動に対応
- 14日間プラン: Day別の問題・合格条件、自動記録・自動完了、CSV出力
- 初心者向けガイド学習: 形を覚える → 2秒ヒント → 自力確認の3フェーズ
- Day別専用レッスン: 転回形、左右手、進行、キー変更、コード譜、初見、総合テスト
- 仮想鍵盤ガイド: 紫の押す鍵盤、緑の正解、赤の余分、左手水色、指番号表示
- 左右手分析: 分割点を設定し、右手転回形と左手ベースを個別表示
- 分数コード: C/E、C/G、D/F#、G/Bなどの右手コードとベースを個別判定
- リアルタイムコード検出: major、minor、dim、sus4、7、maj7、m7、add9と転回形
- Web Audio音源: sine + triangle、16音ポリフォニック、音量・ミュート
- 外部DAW音源: Note On/Off、Velocity、チャンネル、CC、サステイン、Pitch Bend、AftertouchをMIDI出力へ転送
- メトロノーム: アクセント、音量、4カウント
- 集中モード
- 曲で弾く: U-FRETコード自動転記、動画プラス拍マップ／Songle実時間への自動同期、動画追従コードレール、MIDI一致表示、コード先読み、自動転回形、右手／両手判定、速度変更、A/Bループ、同期補正、MP3自動MIDI化、YouTube URL／タイトル検索

## 14日間プランの流れ

- Day 1〜3: ガイドを見ながらコードの形を覚え、最後にガイドなしで2回正解する
- Day 4〜5: 転回形と左手ベース＋右手コード
- Day 6・10: メトロノーム同期のコード進行とキー変更
- Day 7・13: コード瞬発テストと苦手コード集中
- Day 8〜9: 4音コードと分数コード
- Day 11: 自分で入力できるコード譜エディタ
- Day 12: 10秒の確認時間から始まる初見コード譜
- Day 14: 瞬発、転回形、分数、進行、初見の総合テスト

コード別にガイドあり／なし正解数、ミス、ヒント使用、反応時間、最終練習日時、習得状態をブラウザへ保存します。

## 対応ブラウザ

- Google Chrome（最新版）
- Microsoft Edge（最新版）

Web MIDI APIを利用するため、FirefoxとSafariは対象外です。MIDI権限は `localhost` またはHTTPSの安全なコンテキストでのみ利用してください。

## トラブルシューティング

### MIDI機器が見つからない

- キーボードの電源とUSB接続を確認し、ページを再読み込みします。
- DAWなど、MIDI機器を排他的に使用するアプリを終了します。
- USBハブを外し、PC本体のUSBポートへ直接接続します。
- ブラウザのサイト設定でMIDI権限を一度リセットし、再接続します。

### 音が鳴らない

- 画面を一度クリックしてから鍵盤を弾きます。ブラウザはユーザー操作前の音声開始を制限します。
- 左下の音源がミュートになっていないか、音量が0でないか確認します。
- OSの出力先と音量を確認します。

### 音が鳴り続ける

ウィンドウからフォーカスが外れると自動で全音停止します。改善しない場合はページを再読み込みしてください。

### 履歴が保存されない

プライベートブラウズや、サイトデータを終了時に削除する設定では `localStorage` が消える場合があります。通常ウィンドウで使用してください。

## GitHub Pagesへのデプロイ

`main` ブランチへpushすると、[Deploy to GitHub Pages](.github/workflows/deploy-pages.yml) が自動で次を実行します。

1. `npm ci`
2. `npm test`
3. `npm run build`
4. `dist` をGitHub Pagesへデプロイ

Viteの公開パスは相対指定のため、ローカル配信と `/PianoPractice/` 配下のGitHub Pagesの両方で同じビルドを利用できます。公開版では、収録済みU-FRETコード＋動画プラス同期、Songleのタイトル検索・コード自動取得、YouTube URL、自作コード譜、MIDI読込を利用できます。任意のU-FRETページ取得とMP3自動MIDI変換にはローカル版が必要です。手動で再実行する場合は、GitHubの **Actions → Deploy to GitHub Pages → Run workflow** を使用してください。
